<% # container for all documents in index view -%>
<% docs = @response.docs.map { |x| SolrDocument.new(x) } %>
<%= render :partial => "catalog/_index_partials/document_list", :locals => {:recent_documents => docs} %>

