<% if worktype.empty? %>
  <% worktype.build %>
<% end %>
<div id="type">
  <%= f.fields_for :worktype do |t| %>
    <div class="field">
      <%= t.label :typeLabel, "Type", :class=>"control-label" %>
      <%= t.select :typeLabel, options_for_select(Sufia.config.article_types, :selected=>worktype[0].typeLabel.first) %>
    </div>
  <% end %>
</div>
